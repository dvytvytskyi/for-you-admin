# 🚀 Швидкий старт деплою

## Структура файлів деплою

```
deploy/
├── DEPLOY.md              # Повні інструкції деплою
├── setup-server.sh        # Налаштування сервера (перший раз)
├── deploy.sh              # Повний деплой з нуля
├── quick-deploy.sh         # Швидкий деплой після оновлень
├── nginx.conf              # Конфігурація Nginx
└── .env.production.example # Приклад production .env
```

---

## Швидкий деплой (якщо сервер вже налаштований)

1. **Підключитися до сервера:**
```bash
ssh root@135.181.201.185
```

2. **Завантажити проект:**
```bash
cd /opt/admin-panel
# Клонуйте або скопіюйте проект
```

3. **Створити .env файли** (див. `DEPLOY.md`)

4. **Запустити деплой:**
```bash
./deploy/quick-deploy.sh
```

---

## Повний деплой з нуля

Детальні інструкції в [`DEPLOY.md`](./DEPLOY.md)

---

## Ключові файли для production

- `docker-compose.prod.yml` - Production Docker Compose
- `admin-panel-backend/Dockerfile` - Backend образ
- `admin-panel/Dockerfile` - Frontend образ
- `deploy/nginx.conf` - Nginx конфігурація

---

## Потрібна допомога?

Перегляньте [`DEPLOY.md`](./DEPLOY.md) для повної документації.

