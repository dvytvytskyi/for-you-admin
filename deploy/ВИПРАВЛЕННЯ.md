# 🔧 Виправлення: Адмінка показується на основному домені

## ⚡ Швидке виправлення

### Варіант 1: Через SSH (рекомендовано)

```bash
# 1. Підключіться до сервера
ssh root@88.99.38.25
# Пароль: PgTeNqcgnwWu

# 2. Виконайте скрипт
cd /opt/admin-panel
git pull origin main
chmod +x deploy/fix-admin-on-main-domain.sh
./deploy/fix-admin-on-main-domain.sh
```

### Варіант 2: Автоматично з локальної машини

```bash
# З директорії проекту
cd /Users/vytvytskyi/admin_for_you

# Встановіть sshpass (якщо ще не встановлено)
brew install hudochenkov/sshpass/sshpass

# Запустіть скрипт
./deploy/run-fix-remote.sh
```

### Варіант 3: Одна команда через SSH

```bash
ssh root@88.99.38.25 "cd /opt/admin-panel && git pull origin main && chmod +x deploy/fix-admin-on-main-domain.sh && ./deploy/fix-admin-on-main-domain.sh"
```

## 📋 Що робить скрипт

1. ✅ Знаходить конфігурації, де `foryou-realestate.com` проксує на адмінку
2. ✅ Створює резервні копії перед змінами
3. ✅ Видаляє проблемні конфігурації
4. ✅ Виправляє конфігурацію адмінки
5. ✅ Перезапускає nginx

## ✅ Перевірка після виправлення

Відкрийте в браузері:
- ✅ `https://admin.foryou-realestate.com` → має показувати адмінку
- ✅ `https://foryou-realestate.com` → НЕ має показувати адмінку

## 🔍 Додаткові команди для діагностики

```bash
# На сервері
ssh root@88.99.38.25

# Перевірка конфігурацій nginx
ls -la /etc/nginx/sites-enabled/
nginx -T 2>&1 | grep -E "server_name|listen" | grep "foryou-realestate.com"

# Перевірка статусу nginx
systemctl status nginx
```

## 📚 Детальні інструкції

- `RUN_FIX_ON_SERVER.md` - детальні інструкції
- `FIX_ADMIN_ON_MAIN_DOMAIN.md` - повна документація проблеми
- `QUICK_FIX.md` - швидкий гайд
