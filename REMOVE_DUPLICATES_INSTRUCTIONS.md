# 🗑️ ІНСТРУКЦІЯ: ВИДАЛЕННЯ ДУБЛІКАТІВ ПРОЕКТІВ

## 📊 Що буде зроблено:

- **Буде видалено:** 1,254 записи (старіші дублікати)
- **Залишиться:** 1,314 записів (найновіші версії кожного проекту)
- **Критерій:** Залишаємо запис з найновішою датою `updatedAt`

---

## ✅ Перевірка перед видаленням:

Всі дублікати мають різні дати оновлення, тому не буде конфліктів.

**Приклад:**
- **330 Riverside Crescent** (3 записи):
  - Запис 1: оновлено 08.01.2026 09:48 - **БУДЕ ВИДАЛЕНО**
  - Запис 2: оновлено 08.01.2026 09:41 - **БУДЕ ВИДАЛЕНО**  
  - Запис 3: оновлено 12.01.2026 19:54 - **ЗАЛИШИТЬСЯ** ✅

---

## 🚀 Як виконати видалення:

### Крок 1: Попередній перегляд (необов'язково)
```bash
./preview-duplicates-removal.sh
```

### Крок 2: Видалення дублікатів на ПРОДАКШН
```bash
./remove-duplicates-prod.sh --confirm
```

**Скрипт автоматично:**
1. ✅ Створить backup бази даних
2. ✅ Покаже, що буде видалено
3. ✅ Видалить дублікати
4. ✅ Покаже результат

---

## 📋 Що станеться:

### До видалення:
- Всього проектів: **2,568**
- Унікальних назв: **1,314**
- Дублікатів: **1,254**

### Після видалення:
- Всього проектів: **1,314**
- Унікальних назв: **1,314**
- Дублікатів: **0**

---

## 🔒 Безпека:

- ✅ Автоматичний backup перед видаленням
- ✅ Backup зберігається на сервері в `/root/`
- ✅ Назва backup: `backup_before_duplicate_removal_YYYYMMDD_HHMMSS.sql`

### Як відновити backup (якщо потрібно):
```bash
ssh root@135.181.201.185
docker exec -i for-you-admin-panel-postgres-prod psql -U admin admin_panel < /root/backup_before_duplicate_removal_*.sql
```

---

## ⚠️ ВАЖЛИВО:

1. **Backup створюється автоматично** - не потрібно робити вручну
2. **Видаляються тільки старіші версії** - найновіші дані залишаються
3. **Після видалення рекомендується** додати унікальний індекс:

```sql
CREATE UNIQUE INDEX idx_properties_name_unique ON properties(name);
```

Це запобіжить створенню нових дублікатів в майбутньому.

---

## 📝 Команди:

```bash
# 1. Попередній перегляд (опціонально)
./preview-duplicates-removal.sh

# 2. Видалення дублікатів (з автоматичним backup)
./remove-duplicates-prod.sh --confirm
```

---

## ✅ Готово до виконання!

Просто запустіть команду з `--confirm` коли будете готові.
